/**
 * Game configuration
 */

/**
 * Toggle between single-word mode and sentence mode
 * When true: Use sentence generation from high-interest words
 * When false: Use existing single-word lists
 */
export const USE_SENTENCES = true;
